# Deploy Springboot using Docker and Nginx

```
Docker Compose + Spring Boot + Nginx
```

## Installation and Setup Instructions
1. Clone down this repository. you will need `Docker` installed and `logged in` on your machine.
2. Run: `docker-compose up -d` in Terminal


